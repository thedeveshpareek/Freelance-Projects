import React from 'react'

const index = ({inputText}) => {
    return (
        <input className='bg-transparent p-3 w-full text-gray-600 focus:outline-none' placeholder={inputText}/>
    )
}

export default index